fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
httpx==0.27.2
pydantic==2.9.2
google-generativeai==0.8.3
python-multipart==0.0.9


[x] Fix 1 — Change admin key from admin123 to random key
[ ] Fix 2 — Add Twilio webhook signature verification
[ ] Fix 3 — Replace assert with if in update_meeting_session
[ ] Fix 4 — Add input length caps to meeting flow fields
[ ] Fix 5 — Add LLM prompt injection guardrails
[ ] Fix 6 — Add CORS middleware
